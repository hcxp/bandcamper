addons:
  postgresql: 9.6
before_script:
  - psql -c 'create database bandcamp_api_test;' -U postgres
dist: trusty
language: ruby
rvm:
  - 2.5.0
script:
  - RAILS_ENV=test bundle exec hanami db prepare
  - bundle exec rspec
